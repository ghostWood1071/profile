# profile
--folder public dùng để chứa các file tĩnh: image, css, js
--folder views dùng để chứa các trang html, --code vẫn là html chỉ việc đổi thành .ejs
-- !!!!! lưu ý với các đường dẫn tới file tĩnh stylesheet(css), js, trong file ejs có dạng: 
  +css: /stylesheets/file.css
  +js: javascripts/file.js
  +images: images/imgae.jpg
--folder bussiness dùng để chứa các middleware xử lý
--folder data dùng để chứa các file data.json
--folder routes chứa các router
